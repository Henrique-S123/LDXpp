:te union{#a : int} ? union{#b : int};;

:te union{#a : int} ? union{#a : linint};;

:te union{#a : int} ? union{#a : int};;

:te union{#a : int, #b : bool} ? union{#a : int};;

:te union{#a : int, #b : bool} ? union{#a : int, #c : bool};;

:te union{#a : int, #b : bool} ? union{#a : int, #b : bool};;

:te union{#a : int, #b : bool} ? union{#b : bool, #a : int};;
