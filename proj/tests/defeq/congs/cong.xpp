let congUnit:(x:())->(x = () : ()) =
    fn x:() => { x; refl };
congUnit(());;

let congNonlinPair:(x:(int, int))->(p:(int, int), (x = p : (int, int))) =
    fn x:(int, int) => {
        let y = fst(x); let z = snd(x); ((y, z), refl)
    };
congNonlinPair((2, 3));;
